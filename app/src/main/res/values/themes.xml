<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BottomNavigation" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorOnPrimary</item>
        <item name="colorPrimaryVariant">@color/colorOnPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/color_mainBackground</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorOnPrimary</item>
        <item name="colorPrimaryDark">@color/color_mainBackground</item>
        <item name="colorAccent">@color/colorOnPrimary</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <style name="ToggleButtonGroupStyle" parent="Theme.MaterialComponents">
        <item name="backgroundTint">@color/button_togglegroup_color</item>
        <item name="android:textColor">@color/button_togglegroup_textcolor</item>
        <item name="android:textStyle">bold</item>
        <item name="strokeColor">@color/color_buttonSelected</item>
        <item name="strokeWidth">1dp</item>
    </style>

</resources>